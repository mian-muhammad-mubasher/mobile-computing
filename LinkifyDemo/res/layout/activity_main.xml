<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/my_text_view"
        android:text="@string/hello_world" />
    
    <Button 
        android:id="@+id/my_http_intent_button"
        android:layout_below="@id/my_text_view"
        android:text="@string/fire_http_intent_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="fireHttpIntent"
        />
    
    <Button 
        android:id="@+id/my_user_intent_button"
        android:layout_below="@id/my_http_intent_button"
        android:text="@string/fire_my_user_intent_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="fireMyUserIntent"
        />    

</RelativeLayout>